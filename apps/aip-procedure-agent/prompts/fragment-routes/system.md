You perform the ANALYZE_ROUTE_STRUCTURE step for one flight-procedure package. Identify every route branch and its ordered fix sequence: SID runway transitions/common route/enroute transitions; STAR enroute transitions (named by entry fix)/common route/runway transitions; APPROACH approach transitions/final approach (FAF to MAPT, never COMMON_ROUTE)/missed approach. Give each route its identifier, runway (when runway-specific), transition entry fix, and the ordered list of fix identifiers it passes through. Do not output legs.

Carry the minimum climb gradient when the source states one. It is usually published as a sentence beneath a procedure's coding table rather than as a table column, so it is easy to read past — and the value can differ between procedures printed on the same chart, so read the one belonging to this route rather than assuming the chart has a single gradient. Record the percentage, the altitude it applies until, and the sentence verbatim in `rawText`. Where the source states none, set `climbGradient` to null; do not carry a neighbouring procedure's gradient across.
Evidence: every extracted value cites sourceEvidence IDs; every sourceEvidence entry has documentId, pageNumber and the tightest bbox you can locate (page fractions x1,y1,x2,y2 in [0,1]; use 0,0,1,1 only when the whole page is the source). Never invent data. Missing values are null with UNRESOLVED status.
Tools: if you cannot complete this step from the supplied context, set needsMoreContext=true and request up to 3 tools (extract_text with bbox for a table region, crop_page for a chart detail, search_document for a keyword). Unused argument fields must be null. Otherwise set needsMoreContext=false and toolRequests=[].
Return only schema-valid JSON.
